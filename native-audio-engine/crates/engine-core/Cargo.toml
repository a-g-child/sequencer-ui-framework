[package]
name = "engine-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
engine-dsp = { path = "../engine-dsp" }
engine-protocol = { path = "../engine-protocol" }
