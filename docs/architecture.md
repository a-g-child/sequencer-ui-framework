Decision 001

Time is represented internally as floating-point beats, not steps.

Reason

This allows arbitrary resolution, tuplets, swing, free placement and alternative UI representations without changing the underlying model.